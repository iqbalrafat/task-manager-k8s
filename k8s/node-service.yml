apiVersion: v1
kind: Service
metadata:
  name: task-api
  namespace: task-app
spec:
  type: NodePort
  selector:
    app: task-api
  ports:
    - port: 3002
      targetPort: 3002
      nodePort: 30002
